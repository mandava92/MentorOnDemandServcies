spring:
  application:
    name: discovery-server
  cloud:
    config:
      enabled: true
      fail-fast: true
      uri: http://localhost:8092
eureka:
  client: 
    registerWithEureka: false
    fetchRegistry: false      
  